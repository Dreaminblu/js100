// Predict the output of the below code.
// When you run the code, do you see what you expected? Why or why not?

let array1 = [2, 6, 4];
let array2 = [2, 6, 4];

console.log(array1 === array2);

// It logs false because array1 and array2 are different objects in memory, even though they have the same contents.
